# 1. Conexión a tu Base de Datos PostgreSQL local
spring.datasource.url=jdbc:postgresql://localhost:5432/veltrix_db
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=JOSElayme#0

# 2. Configuración de Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# 3. Nombre de la aplicación
spring.application.name=veltrix

# 4. Debug de seguridad
logging.level.org.springframework.security=DEBUG